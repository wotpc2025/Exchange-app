services:
  exchange-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: exchange-app-prod
    restart: unless-stopped
    ports:
      - "3022:3000"
    environment:
      # App Config
      - NODE_ENV=production
    env_file:
      - .env